from django.apps import AppConfig


class HealthWeatherConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.health_weather'
    verbose_name = 'Health Weather'
